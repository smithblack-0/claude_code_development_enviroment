"""
Initial sync setup — copies sync.py into the project and runs initial index.

Stage 3 stub: will copy templates/sync.py to rag/sync.py and invoke it.
"""

from pathlib import Path


def run_initial_sync(project_root: Path) -> None:
    """STUB (Stage 3): Copy sync script and run initial index population."""
    print("\n[stub] run_initial_sync — will copy and run rag/sync.py (Stage 3)")
